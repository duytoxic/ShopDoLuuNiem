@model IEnumerable<ShopThoiTrang.Models.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-md-6">
                <strong class="text-uppercase text-danger">Thùng rác sản phẩm</strong>
            </div>
            <div class="col-md-6 text-right">
                <a href="@Url.Action("Index", "Product")" class="btn btn-sm btn-success">
                    <i class="far fa-chevron-left"></i> Quay lại danh sách
                </a>
            </div>
        </div>
    </div>
    <div class="card-body p-3">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th style="width: 15px" class="text-center">ID</th>
                    <th style="width: 80px" class="text-center">Hình ảnh</th>
                    <th style="width: 120px" class="text-center">Tên sản phẩm</th>
                    <th style="width: 20px" class="text-center">Giá</th>
                    <th style="width: 20px" class="text-center">Giá sale</th>
                    <th style="width: 15px" class="text-center">Trạng thái</th>
                    <th style="width: 120px" class="text-center">Chức năng</th>
                </tr>
            </thead>
            <tbody>
               @foreach (var item in Model)
                {
                <tr>
                    <td class="text-center">@item.Id</td>
                    <td class="image-product-container text-center">
                        <img src="~/Public/images/product/@item.Img" />
                    </td>
                    <td>@item.Name</td>
                    <td>@item.Price đ</td>
                    <td>@item.Pricesale đ</td>
                    <td class="text-center">
                        @if (item.Status == 1)
                        {
                            <a href="@Url.Action("Status", "Product", new { @id = item.Id })" class="switch">
                                <input type="checkbox" checked>
                                <span class="slider round"></span>
                            </a>
                        }
                        else
                        {
                            <a href="@Url.Action("Status", "Product", new { @id = item.Id })" class="switch">
                                <input type="checkbox">
                                <span class="slider round"></span>
                            </a>
                        }
                    </td>
                    <td class="text-center">
                        <a href="@Url.Action("ReTrash", "Product", new { @id = item.Id })" class="btn btn-sm btn-outline-success">
                            Khôi phục
                        </a>
                        <a href="@Url.Action("Delete", "Product", new { @id = item.Id })" class="btn btn-sm btn-danger">
                            Xác nhận
                        </a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>

